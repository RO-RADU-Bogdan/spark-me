version: '2'
services:
  sparkme-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/SparkMe/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=SparkMe
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
